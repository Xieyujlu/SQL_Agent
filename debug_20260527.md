# debug_20260527

## 功能：Human-in-the-Loop（HITL）SQL 查询审核

为 SQLAgent-ms 增加人工审核功能：每次 SQL 查询执行后，通过 LangGraph `interrupt()` 挂起执行，前端展示 SQL 语句、查询结果和三个审核按钮（准确/错误/其他建议），用户反馈后继续执行。

---

## Bug 列表

### 1. 前端卡住，查询后不弹出审核框

**现象**：发送查询后前端无任何响应，一直处于 loading 状态。

**根因**：`stream_mode=["messages", "updates"]` 改变了 `astream` 返回的 StreamChunk 格式。StreamChunk 是 **3 元组** `(namespace, mode, data)`，但代码按 2 元组解析：
```python
# 错误
mode, data = chunk
# 正确
mode = chunk[1]
data = chunk[2]
```

**修复**：`agent_runner.py` `_stream()` 中改为 3 元组解构。

---

### 2. interrupt() 被工具层 try-except 吞噬

**现象**：`interrupt()` 被调用但没有向上传播，图没有真正挂起。

**根因**：`SQLQueryTool._run()` 中的 `except Exception as e:` 捕获了 `GraphInterrupt`。继承链：
```
GraphInterrupt → GraphBubbleUp → Exception
```
`GraphInterrupt` 最终继承自 `Exception`，被通用异常捕获吞掉了。

**修复**：[tool.py:127](src/agent/tools/tool.py#L127) 在 `except Exception` 前增加 `except GraphInterrupt: raise`，确保中断信号向上传播。

---

### 3. HITL 中断事件未被检测到

**现象**：`interrupt()` 被正确调用（log 可见），但前端收不到 `hitl_required` 事件。

**根因**：同 Bug 1，StreamChunk 用 2 元组解析，导致 `mode` 和 `data` 取值错位，`updates` 模式的 `__interrupt__` 检测永远走不到。

**修复**：修正 StreamChunk 为 3 元组解析后解决。

---

### 4. 恢复对话后重复触发 HITL

**现象**：用户点击审核按钮提交反馈后，`resume_chat()` 恢复执行时又触发一次 `interrupt()`，再次弹出审核按钮。

**根因**：LangGraph 的 `interrupt()` 恢复后会让所在节点**重新执行**。这意味着 ToolNode 会重新调用 `SQLQueryTool._run()`，SQL 重新执行一次，`_request_hitl()` 再次调用 `interrupt()`。

**修复**：`_stream()` 新增 `enable_hitl` 参数：
- `astream_chat()` 传 `enable_hitl=True`（正常首次查询，允许中断）
- `resume_chat()` 传 `enable_hitl=False`（恢复执行，跳过中断检测）

---

### 5. `db_utils.py` KeyError: 'columns'

**现象**：获取表模式信息时报 `KeyError: 'columns'`。

**根因**：SQLAlchemy `Inspector.get_indexes()` 返回的字典中，索引列名字段是 `column_names` 而不是 `columns`。代码写了 `idx['columns']`。

**修复**：[db_utils.py:112](src/agent/utils/db_utils.py#L112) → `idx['column_names']`

---

### 6. 弹出审核按钮前没有任何文字内容显示

**现象**：用户问完问题后，聊天区域直接出现审核按钮，但上面没有任何 Agent 的思考文字（如"正在查询数据库..."）。

**根因**：`_stream()` 中对 AI token 的过滤条件过于严格：
```python
if (
    token.content
    and hasattr(token, "tool_call_chunks")
    and not token.tool_call_chunks  # ← 这个条件拦截了所有消息
):
    yield token.content
```
qwen3.5-plus 模型在生成 tool call 时，`token.tool_call_chunks` 非空，导致所有思考文本都被过滤掉。

**修复**：[agent_runner.py:98](src/backend/agent_runner.py#L98) 移除 `not token.tool_call_chunks` 条件，仅保留 `token.content` 和 `hasattr` 检查。

---

### 7. 审核按钮上方不显示 SQL 语句和查询结果

**现象**：审核按钮正常弹出，但用户看不到执行的 SQL 语句和查询结果，无法判断数据是否正确。

**根因**：`__interrupt__` 数据中包含 `{"query": ..., "result": ..., "options": [...]}`，但后端检测到中断后只发送了 `session_id`，没有提取 query 和 result 传给前端。

**修复**：
- 后端：从 `interrupt_data[0].value` 中提取 `query` 和 `result`，放入 `hitl_required` 事件
- 前端：`createFeedbackUI()` 在按钮上方渲染 SQL 语句（深色代码块）和查询结果（浅灰背景块）

---

## 涉及文件

| 文件 | 修改内容 |
|------|----------|
| [src/agent/tools/tool.py](src/agent/tools/tool.py) | 新增 `_request_hitl()`，`GraphInterrupt` 不被捕获 |
| [src/backend/agent_runner.py](src/backend/agent_runner.py) | StreamChunk 3 元组解析，HITL 检测与恢复，query/result 提取 |
| [src/backend/main.py](src/backend/main.py) | 新增 `POST /api/chat/feedback` 端点 |
| [src/agent/utils/db_utils.py](src/agent/utils/db_utils.py) | `idx['columns']` → `idx['column_names']` |
| [src/agent/utils/feedback_db.py](src/agent/utils/feedback_db.py) | 新建，MySQL 反馈存储 |
| [src/multi_agent/multi_agent.py](src/multi_agent/multi_agent.py) | SQL 子 agent 传入 `checkpointer`，更新 system prompt |
| [src/frontend/app.js](src/frontend/app.js) | HITL 审核 UI，SQL/结果展示，反馈提交 |
| [src/frontend/style.css](src/frontend/style.css) | 审核按钮、SQL 代码块、结果块样式 |

## 关键经验

1. **LangGraph StreamChunk 是 3 元组 `(namespace, mode, data)`**，使用 `subgraphs=True` 和复合 `stream_mode` 时尤其要注意解构顺序。
2. **`interrupt()` 抛出的 `GraphInterrupt` 继承自 `Exception`**，工具代码中 `except Exception` 会吞噬中断信号，必须显式 `except GraphInterrupt: raise`。
3. **子 agent 的 `create_agent()` 必须传入 `checkpointer`**，否则子图无法使用 `interrupt()`。
4. **`interrupt()` 恢复后会重入当前节点**，SQL 会被执行两次（对 SELECT 安全），需要 `enable_hitl` 标记防止重复中断。
5. **不同 LLM 的 `token.tool_call_chunks` 行为不同**，qwen3.5-plus 直接在含 tool call 的消息中设该字段，过滤条件不能假设文本 token 的 `tool_call_chunks` 为空。
